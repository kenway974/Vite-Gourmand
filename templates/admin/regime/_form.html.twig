{{ form_errors(form) }}

{{ form_start(form) }}
    {{ form_row(form.libelle) }}
    {{ form_row(form.description) }}
    <button type="submit">{{ bouton }}</button>
{{ form_end(form) }}

<p><a href="{{ path('app_admin_regime_index') }}">Retour à la liste</a></p>
